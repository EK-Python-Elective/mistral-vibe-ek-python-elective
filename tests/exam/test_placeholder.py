from __future__ import annotations

# Placeholder so `pytest tests/exam` has something to collect before your
# feature's tests exist. Delete this once you've added your own tests.


def test_placeholder() -> None:
    assert True
